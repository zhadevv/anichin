## What does this PR do?



## Why

What prompted this change: a bug report, a markup change on Anichin's
side, a new endpoint, etc.

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run test:unit` passes
- [ ] If this touches a parser, a fixture was added/updated under
      `test/fixtures/` and a matching test in `test/parser/`
- [ ] If this changes response shape, `docs/METHODS.md` (and
      `CHANGELOG.md`) were updated
- [ ] If this is a breaking change, it's called out explicitly in the PR
      description and in `CHANGELOG.md`
